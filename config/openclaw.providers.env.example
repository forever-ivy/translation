# OpenClaw provider secrets (do not load into n8n)
# Put these in shell env before starting openclaw gateway,
# or configure via `openclaw models auth` commands.

# Primary + reviewer chain
# Codex uses OAuth profile (openai-codex), so no API key required for primary.
GEMINI_API_KEY=

# Optional fallback relay (only if you decide to add more providers later)
OPENROUTER_API_KEY=
